-- =====================================================================
-- POPULATE ALL REFERENCE TABLES - COMBINED
-- NO \i COMMANDS - Ready for Supabase SQL Editor
-- Creates: Therapeutic Areas, Disease Areas, Company Sizes, AI Maturity,
--          VPANES, Stakeholders, Responsibilities, KPIs, Skills
-- Note: Tools table already exists with 94 rows, skipping
-- =====================================================================

DO $$
BEGIN
    RAISE NOTICE '=================================================================';
    RAISE NOTICE 'POPULATING ALL REFERENCE TABLES (COMBINED SCRIPT)';
    RAISE NOTICE '=================================================================';
    RAISE NOTICE '';
END $$;

-- Copy the first script contents here, then the second
-- This is ready to run in Supabase SQL Editor!

-- Run this instead of the master script with \i commands

