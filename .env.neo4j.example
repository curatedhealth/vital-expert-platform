# ==================== Neo4j Configuration ====================
# Neo4j AuraDB or Self-Hosted Configuration
# Required for GraphRAG hybrid search (20% weight)

# Production (Neo4j AuraDB)
NEO4J_URI=neo4j+s://xxxxx.databases.neo4j.io
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-secure-password-here
NEO4J_DATABASE=neo4j

# Development (Local Docker)
# NEO4J_URI=neo4j://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=development-password
# NEO4J_DATABASE=neo4j

# ==================== Pinecone Configuration ====================
# Updated for GraphRAG - now includes agent embeddings index

PINECONE_API_KEY=your-pinecone-api-key
PINECONE_INDEX_NAME=vital-knowledge
PINECONE_AGENT_INDEX_NAME=vital-agents  # NEW: Dedicated index for agent embeddings
PINECONE_ENVIRONMENT=us-west1-gcp  # Your Pinecone environment

# ==================== Existing Configuration ====================
# (Keep your existing SUPABASE, OPENAI, etc. configuration)

# Supabase
SUPABASE_URL=your-supabase-url
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# OpenAI
OPENAI_API_KEY=your-openai-key
OPENAI_MODEL=gpt-4-turbo-preview
OPENAI_EMBEDDING_MODEL=text-embedding-3-large

# Redis (for caching)
REDIS_URL=redis://localhost:6379

# ==================== GraphRAG Performance Settings ====================

# Performance targets
GRAPHRAG_P95_LATENCY_TARGET=450  # milliseconds
GRAPHRAG_MIN_CONFIDENCE=0.70  # Minimum confidence threshold

# Cache settings
GRAPHRAG_CACHE_TTL=3600  # seconds (1 hour)
GRAPHRAG_CACHE_ENABLED=true

# ==================== Sub-Agent Configuration ====================

# Resource limits
MAX_CONCURRENT_SUB_AGENTS=20
SUB_AGENT_SPAWNING_TIMEOUT=500  # milliseconds
SUB_AGENT_EXECUTION_TIMEOUT=30000  # milliseconds (30 seconds)

# Model configuration for sub-agents
SUB_AGENT_DEFAULT_MODEL=gpt-4
SUB_AGENT_SPECIALIST_MODEL=gpt-4
SUB_AGENT_WORKER_MODEL=gpt-4-turbo-preview
SUB_AGENT_TOOL_MODEL=gpt-3.5-turbo
