# Temporarily exclude problematic pages to get initial deployment working
# These can be re-enabled after fixing the issues

# Pages with syntax errors or client-side server imports
src/app/(app)/langgraph-studio/**
src/app/(app)/knowledge-domains/**
src/app/(app)/workflow-designer/**

# Components causing issues
src/components/langgraph-visualizer.tsx

# Workflow API routes with wrong import paths
src/app/api/workflows/**
src/app/api/executions/**

# Sentry config (missing package)
sentry.*.ts
instrumentation.ts

