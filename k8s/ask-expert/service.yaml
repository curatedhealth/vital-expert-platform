apiVersion: v1
kind: Service
metadata:
  name: ask-expert
  namespace: vital-production
  labels:
    app: ask-expert
    component: ask-expert
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: ask-expert
